<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <style>
         body, html {
    height: 100%;
    margin: 0;
    display: flex;
    align-items: center;  /* 세로 방향 중앙 정렬 */
    justify-content: center; /* 가로 방향 중앙 정렬 */
  }
        .bgVideo {
      position: fixed;
      right: 0;
      bottom: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: -1;
    }
    #Login {
    text-align: center;
    font-size: 40px;
    color: red;
    cursor: pointer; /* 클릭 가능한 요소임을 나타내는 커서 스타일 */
    z-index: 1;
  }
  </style>
</head>
<body>
    <video autoplay muted loop class="bgVideo"; >
        <source src="../../../static/img/Login.mp4" type="video/mp4">
        Your browser does not support HTML5 video.
      </video>
    <p id="Login" onclick="location.href='/auth/loginForm'" >로그인</p>
    
</body>
</html>