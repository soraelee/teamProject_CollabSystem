<!DOCTYPE html>
  <!-- this file is not being used-->

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chart Page</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
      
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
</head>
<body>

    
        
<div id="charts">
    <% for(let i = 0; i < teamList.length; i++) { %>
        <a hidden id="tl<%- i %>" data-team-list='<%- JSON.stringify(teamList[i]) %>'></a>
    <% } %>

    <a hidden id="tLength" team-list-length='<%- JSON.stringify(teamList.length) %>'></a>

    <% for(let i = 0; i < contributionsByRound.length; i++) { %>
        <a hidden id="bd<%- i %>" data-contribution-list='<%- JSON.stringify(contributionsByRound[i]) %>'></a>
        <div id="employee_piechart<%- i %>" style="width: 500px; height: 500px; float: left;"></div>
    <% } %>
</div>


    <!-- <script>
        window.onload = () => {
            console.log("Window loaded");
            google.load("visualization", "1", {packages:["corechart"]});
            google.setOnLoadCallback(drawCharts);
        }
        
        function drawCharts() {
            console.log("Drawing charts");
            let tLength = JSON.parse(document.getElementById("tLength").getAttribute("team-list-length"));
            console.log("tLength:", tLength);
            for(let i = 0; i < tLength; i++) {
                let contributionsData = JSON.parse(document.getElementById("bd" + i).getAttribute("data-contribution-list"));
                let teamListData = JSON.parse(document.getElementById("tl" + i).getAttribute("data-team-list"));
                console.log("Contributions data for chart " + i + ":", contributionsData);
                console.log("teamList " + i + ":", teamListData);
                var data = google.visualization.arrayToDataTable(contributionsData);
                var options = {
                    title: teamListData
                };
                var chart = new google.visualization.PieChart(document.getElementById("employee_piechart" + i));
                chart.draw(data, options);
            }
        }
    </script> -->
  
 <!-- <script>   for fetch  (just in case)
    
    function selectButton(button) {
      let teamName = JSON.parse(button.getAttribute("data-team"));
      console.log("teamname:",teamName);
      
      $.ajax({   //ajax 는 controller의 res.json 만 받는다  
        type: "get",
        url: "forEachTeamData",
        data: { team: teamName },
        success: function(response) {
          
          let contributionsForEachTeam = response.contributionsForEachTeam;
          contributionsForEachTeam=JSON.stringify(contributionsForEachTeam);

          console.log("contributionsForEachTeam:",contributionsForEachTeam)
          let currenTeam= JSON.stringify(response.currenTeam);
          
         
          var piechart = document.getElementById("piechart");
        
          
          
          let html = `
            <a hidden id="teamName" data-teamName='${currenTeam}'></a>
            <a hidden id="ft" data-contributionsForEachTeam='${contributionsForEachTeam}'></a>
            <div id="employee_piechart_ForEachTeam" style="width: 500px; height: 500px; float: left;"></div>
          `;

          piechart.innerHTML = html;
          drawChart(currenTeam);
        },
        error: function(error) {
          console.error("Error sending data to server:", error);
        }
      });
    }

 
    function drawChart(teamName) {
      google.charts.load('current', {'packages':['corechart']});
      google.charts.setOnLoadCallback(function() {
       
        
        let contributionsForEachTeamData = JSON.parse(document.getElementById("ft").getAttribute("data-contributionsForEachTeam"));
        console.log("contributionsForEachTeamData:",contributionsForEachTeamData);

        var data = google.visualization.arrayToDataTable(contributionsForEachTeamData);
        var options = {
          title: teamName
        };
        var chart = new google.visualization.PieChart(document.getElementById('employee_piechart_ForEachTeam'));
        chart.draw(data, options);
      });
    }
  </script> -->
</body>

</html>
